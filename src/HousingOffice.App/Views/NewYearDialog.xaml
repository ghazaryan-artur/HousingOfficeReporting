<Window x:Class="HousingOffice.Views.NewYearDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Նոր տարի"
        Height="220" Width="380"
        WindowStartupLocation="CenterOwner"
        FontFamily="{StaticResource AppFont}" FontSize="14"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ինչ տարվա համար ստեղծել նոր տվյալների բազա?" TextWrapping="Wrap" FontWeight="Bold"/>
        <TextBox Grid.Row="1" x:Name="YearBox" Margin="0,10,0,4" Padding="6,4" Style="{StaticResource SearchBox}"/>
        <TextBlock Grid.Row="2" Foreground="#6c757d" FontSize="12" TextWrapping="Wrap"
                   Text="Ընթացիկ տարվա տվյալները չեն փոխանցվի — նոր տարին սկսում է դատարկ բազայով:"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Չեղարկել" Style="{StaticResource ToolbarButtonSecondary}" Click="Cancel_Click" MinWidth="100"/>
            <Button Content="Ստեղծել" Style="{StaticResource ToolbarButton}" Click="Ok_Click" MinWidth="100"/>
        </StackPanel>
    </Grid>
</Window>
